<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.code.baseservice.dao.ZfMerchantRecordDao">

    <resultMap type="com.code.baseservice.entity.ZfMerchantRecord" id="ZfMerchantRecordMap">
        <result property="merchantId" column="merchant_id" jdbcType="INTEGER"/>
        <result property="recordDate" column="record_date" jdbcType="TIMESTAMP"/>
        <result property="rechargeAmount" column="recharge_amount" jdbcType="VARCHAR"/>
        <result property="rechargeTimes" column="recharge_times" jdbcType="INTEGER"/>
        <result property="merchantFee" column="merchant_fee" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ZfMerchantRecordMap">
        select
          merchant_id, record_date, recharge_amount, recharge_times, merchant_fee
        from zf_merchant_record
        where  = #{}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ZfMerchantRecordMap">
        select
          merchant_id, record_date, recharge_amount, recharge_times, merchant_fee
        from zf_merchant_record
        <where>
            <if test="merchantId != null">
                and merchant_id = #{merchantId}
            </if>
            <if test="recordDate != null">
                and record_date = #{recordDate}
            </if>
            <if test="rechargeAmount != null">
                and recharge_amount = #{rechargeAmount}
            </if>
            <if test="rechargeTimes != null">
                and recharge_times = #{rechargeTimes}
            </if>
            <if test="merchantFee != null">
                and merchant_fee = #{merchantFee}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from zf_merchant_record
        <where>
            <if test="merchantId != null">
                and merchant_id = #{merchantId}
            </if>
            <if test="recordDate != null">
                and record_date = #{recordDate}
            </if>
            <if test="rechargeAmount != null">
                and recharge_amount = #{rechargeAmount}
            </if>
            <if test="rechargeTimes != null">
                and recharge_times = #{rechargeTimes}
            </if>
            <if test="merchantFee != null">
                and merchant_fee = #{merchantFee}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="" useGeneratedKeys="true">
        insert into zf_merchant_record(merchant_id, record_date, recharge_amount, recharge_times, merchant_fee)
        values (#{merchantId}, #{recordDate}, #{rechargeAmount}, #{rechargeTimes}, #{merchantFee})
    </insert>

    <insert id="insertBatch" keyProperty="" useGeneratedKeys="true">
        insert into zf_merchant_record(merchant_id, record_date, recharge_amount, recharge_times, merchant_fee)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.merchantId}, #{entity.recordDate}, #{entity.rechargeAmount}, #{entity.rechargeTimes}, #{entity.merchantFee})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="" useGeneratedKeys="true">
        insert into zf_merchant_record(merchant_id, record_date, recharge_amount, recharge_times, merchant_fee)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.merchantId}, #{entity.recordDate}, #{entity.rechargeAmount}, #{entity.rechargeTimes}, #{entity.merchantFee})
        </foreach>
        on duplicate key update
        merchant_id = values(merchant_id),
        record_date = values(record_date),
        recharge_amount = values(recharge_amount),
        recharge_times = values(recharge_times),
        merchant_fee = values(merchant_fee)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update zf_merchant_record
        <set>
            <if test="merchantId != null">
                merchant_id = #{merchantId},
            </if>
            <if test="recordDate != null">
                record_date = #{recordDate},
            </if>
            <if test="rechargeAmount != null">
                recharge_amount = #{rechargeAmount},
            </if>
            <if test="rechargeTimes != null">
                recharge_times = #{rechargeTimes},
            </if>
            <if test="merchantFee != null">
                merchant_fee = #{merchantFee},
            </if>
        </set>
        where  = #{}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from zf_merchant_record where  = #{}
    </delete>

</mapper>

